# Session 2026-04-13 — PDF Sign Extension Setup

## What was done

### Icons
- Replaced old "P" icon with a document+pen signing icon (from user-provided `Ikona podpisu dokumentu.png`)
- Generated all sizes: 16, 32, 48, 128px using sharp
- Removed old SVG attempt

### Fonts
- Removed DancingScript (corrupted files, couldn't get valid TTFs from Google Fonts)
- Now 5 fonts: Helvetica, Times, Courier (standard, Latin only), Roboto, Open Sans (custom, support Cyrillic/Greek)
- Added font language support info in editor sidebar
- Improved error message when font can't encode characters (orange warning popup with font name and suggestion)

### Auth — Google Sign-In + Supabase
- Google Cloud Console: created Web Application OAuth client (not Chrome App — Chrome App has no secret)
- Supabase: enabled Google provider with Client ID + Secret
- Added extension redirect URLs to Supabase: `https://<extension-id>.chromiumapp.org/**`
- Dev extension IDs used: `nchaboiihonhojdlgojkihcecpilmkbc`, `hnehoelemmhbihjkgadnapnndekolgpc`
- OAuth consent screen: Testing mode, test user `molchanovnik7@gmail.com`
- Note: for production need OAuth verification from Google (users see "This app isn't verified" without it)

### Supabase Edge Functions
- Redeployed `check-limit` and `sign-count` with `--no-verify-jwt` (was returning 401)
- Functions: `check-limit`, `sign-count`, `verify-sub` — all ACTIVE
- Project ref: `zeavnlowvyvewvysfxmj`, project name: `pdf-sign`

### Payments — ExtensionPay + Stripe
- Registered on extensionpay.com, extension ID: `pdf-sign`
- Plans: $2.99/month, $29.99/year
- Connected Stripe account (email: molchanovnik7@gmail.com)
- Enabled live Stripe customer portal
- Integrated ExtPay npm package into extension:
  - `src/background.ts` — `ExtPay('pdf-sign').startBackground()`
  - `src/lib/payments.ts` — import from `extpay` npm package, returns ProStatus with paid/paidAt/subscriptionCancelAt
  - Sign flow: check ExtPay first, if paid → skip Supabase limit, if not → check Supabase free limit
- Test payment works with card 4242 4242 4242 4242

### Chrome Web Store
- Developer account: $5 paid
- Extension uploaded and submitted for review (status: pending)
- Description, category (Tools), language (English US), icon, screenshots filled in
- Privacy policy: hosted on GitHub Pages at `https://molchanik.github.io/pdf-sign-extension/privacy-policy.html`
- Repo made public for GitHub Pages to work

### UX Changes
- Added "Continue editing" button after successful download (next to "Sign another PDF")
- Removed watermark entirely (was "Signed with PDF Sign" on last page)
- Font error popup: orange warning triangle, "Unsupported characters", names the font, suggests Roboto/Open Sans
- General error popup: red X circle
- Updated Paywall text: $2.99/mo or $29.99/yr, removed "No watermark" from features
- Options page: shows subscription status, "Manage subscription" link, fixed text "1 file" instead of "3 signatures/month"

### Debug cleanup
- Removed all `console.log("[PDF Sign]...")` debug statements
- Kept `alert()` on sign-in failure (useful for users)

## What's NOT done / needs attention
1. **OAuth verification** — Google shows "This app isn't verified" to users. Need to verify in Google Cloud Console
2. **Stripe verification** — may need additional business info in Stripe Dashboard
3. **Chrome Web Store review** — waiting for approval
4. **Prod zip needs re-upload** — current zip in `build/pdf-sign-extension.zip` has all latest changes, upload when CWS approves first version
5. **Supabase free tier** — project sleeps after 7 days of inactivity, dies after 90 days. Need active users or $25/mo Pro plan

## Key URLs & IDs
- Supabase project: `zeavnlowvyvewvysfxmj` (pdf-sign)
- Supabase URL: `https://zeavnlowvyvewvysfxmj.supabase.co`
- Google OAuth Client ID: `836027625583-ire2heun65hvqff1jk7aarokak6l8dms.apps.googleusercontent.com` (this is the Chrome App one — NOT used by Supabase)
- ExtensionPay ID: `pdf-sign`
- GitHub repo: `github.com/molchanik/pdf-sign-extension` (public)
- Privacy policy: `https://molchanik.github.io/pdf-sign-extension/privacy-policy.html`
- Chrome Web Store Developer Dashboard: `https://chrome.google.com/webstore/devconsole`

## Environment
- `.env.local` contains: `PLASMO_PUBLIC_SUPABASE_URL`, `PLASMO_PUBLIC_SUPABASE_ANON_KEY`, `PLASMO_PUBLIC_EXTPAY_ID`
- Build: Plasmo v0.89.5
- Package manager: npm (pnpm not installed)

## Git commits this session
- `6968918` — fixes (icons, fonts, sidebar, editor changes)
- `6fdc486` — Add privacy policy for Chrome Web Store submission
- `940c1e6` — Add HTML privacy policy for GitHub Pages
- `de5bb1e` — feat: integrate ExtensionPay, remove watermark, improve error UX
